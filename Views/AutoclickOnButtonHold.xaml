<UserControl x:Class="Useful_Autoclicker.Views.AutoclickOnButtonHold"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
             xmlns:local="clr-namespace:Useful_Autoclicker.Views"
             mc:Ignorable="d"
             d:DesignHeight="32"
             d:DesignWidth="185">
    <Border>
        <Grid>
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="*" />
                <ColumnDefinition Width="auto" />
            </Grid.ColumnDefinitions>

            <TextBlock Grid.Column="0"
                       Text="Autoclick on button hold: "
                       Style="{StaticResource RowText}" />
            <CheckBox Grid.Column="1"
                      Style="{StaticResource DefaultCheckBox}"
                     Margin="10,0,5,0" />
        </Grid>
    </Border>
</UserControl>